{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Empty: resolve peers via dist `types` — `@stainless-code/source` pulls Solid JSX into this React tsc.
    "customConditions": []
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "tsdown.config.ts"]
}
