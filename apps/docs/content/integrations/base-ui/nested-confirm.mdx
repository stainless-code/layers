---
title: Nested confirm + Base UI
description: Compose Layers with Base UI for a child confirm inside a parent dialog.
search:
  tags: ["integration"]
---

import buSrc from "../../../recipes/nested-confirm/react.baseui.tsx?raw";

Open a child confirm inside a parent via a Layers layer group — two controlled `Dialog.Root`s (Base UI supports native nesting); one layer per dialog; `call.end` resolves each. See the [live demo](/examples/nested-confirm) for the behavior.

## Recipe

<CodeBlock code={buSrc} lang="tsx" />

## Seam notes

- Don't use `Dialog.Trigger` — Layers owns the imperative open; the layer component renders the controlled overlay directly while mounted.
- Nest via the Layers layer group (`useLayerGroup` + `<group.Outlet />`), one `Dialog.Root` per layer — not stacked-in-one-component.
- **Exit-animation crux** — `onOpenChangeComplete(false)` → `call.end` per dialog; Base UI owns portal/focus/scroll-lock.

## Learn more

- [Nested confirm example](/examples/nested-confirm) — live demo and per-framework recipes
- [Lifecycle](/concepts/lifecycle) — phases, transitions, and dismissal
