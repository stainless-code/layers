import { defineMeta } from "blume";

export default defineMeta({
  title: "Base UI",
  icon: "/icons/base-ui.svg",
  pages: ["toast", "confirm-dialog", "drawer", "nested-confirm", "animated-enter-exit"],
});
