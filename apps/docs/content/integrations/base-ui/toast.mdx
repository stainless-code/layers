---
title: Toast + Base UI
description: Compose Layers with Base UI for fire-and-forget toasts.
search:
  tags: ["integration"]
---

import buSrc from "../../../recipes/toast/react.baseui.tsx?raw";

`void client.open({ ...toast, payload: { message: "Saved" } })` is the typed entry; the layer component renders Base UI's `Toast` primitives while mounted. See the [live demo](/examples/toast) for the behavior.

## Recipe

<CodeBlock code={buSrc} lang="tsx" />

## Seam notes

- Don't use Base UI's trigger wrappers — Layers owns the imperative open; the layer component renders the controlled overlay directly while mounted.
- `Toast.createToastManager()` owns the queue/viewport — lib owns queue; Layers is the typed `open` entry. Don't dual-queue.
- Toast is instant-dismiss (no exit-animation crux) — `call.dismiss()` after the timeout is fine; the `transition`/`call.settle()` axis is irrelevant here (it matters for animated-enter-exit, not toast).
- Lib owns focus/portal/scroll-lock — don't double-apply Layers-side equivalents.

## Learn more

- [Toast example](/examples/toast) — live demo and per-framework recipes
- [Lifecycle](/concepts/lifecycle) — phases, transitions, and dismissal
