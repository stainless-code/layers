---
title: Adapters
description: One framework, one page — per-adapter guides and parity matrix.
search:
  tags: ["adapter"]
---

import Card from "blume/components/content/Card.astro";
import CardGroup from "blume/components/content/CardGroup.astro";

The [core](/adapters/core) is framework-free — see [Overview](/concepts/overview) for the engine model. Each adapter re-exports it and binds stack snapshots to framework reactivity.

## Adapter ergonomics

Every adapter exposes the same primitives (client context, `useLayerClient`, `useStack` with selector + `compare`, `useLayer`) and re-exports the full core — **engine and primitive parity**. Ergonomic wrappers (`StackOutlet`, `createStackHook`, …) are sugar, not extra engine capability; they diverge by design (matrix below). **Angular and Svelte** ship compiler-free and render via imperative / primitive APIs instead of shipped components.

| Capability                                             | React | Preact | Solid | Angular | Vue | Svelte (runes) | Svelte (store) |
| ------------------------------------------------------ | :---: | :----: | :---: | :-----: | :-: | :------------: | :------------: |
| Primitives + core re-export                            |  ✅   |   ✅   |  ✅   |   ✅    | ✅  |       ✅       |       ✅       |
| `StackOutlet` (built-in host)                          |  ✅   |   ✅   |  ✅   |   ✅¹   | ✅  |       —⁴       |       —⁴       |
| `StackSubscribe` (render-prop / scoped slot)           |  ✅   |   ✅   |  ✅   |   —²    | ✅  |       —⁴       |       —⁴       |
| `useStackHandles` (headless `{ states, getCall }`)     |  ✅   |   ✅   |  ✅   |   ✅    | ✅  |       —⁵       |       —⁵       |
| `useMutationFlow` (async-action helper)                |  ✅   |   ✅   |  ✅   |   ✅    | ✅  |       ✅       |       ✅       |
| `useLayerGroup` (nested-stack hook)                    |  ✅   |   ✅   |  ✅   |   ✅³   | ✅  |      ✅⁶       |      ✅⁶       |
| `createStackHook` (`useAppStack`/`AppHost`/`AppLayer`) |  ✅   |   ✅   |  ✅   |   ✅³   | ✅  |       —⁴       |       —⁴       |

¹ Angular ships `renderStack(vcr)` — a compiler-free imperative outlet (`ViewContainerRef.createComponent` + `setInput`, id-keyed so state changes update inputs without recreating components). ² Angular has no `StackSubscribe`; its `useStack(stack, selector)` signal is the idiomatic equivalent. ³ Angular's `useLayerGroup`/`createStackHook` expose `renderInto(vcr)` instead of an `Outlet` component; `createStackHook` returns `{ provideClient, useAppStack, renderInto }` (no `AppHost`/`AppLayer`). ⁴ Svelte ships no `.svelte` components: render with `{#each useStack().current as s}` + `callFor(s)` (runes) / `{#each $stack}` + `callFor(client, stackId, s)` (store) in your own markup. ⁵ Svelte's `useStack()` already returns the headless pair — `.current` (states) + `callFor`. ⁶ Svelte's `useLayerGroup` exposes the child `stack` (`SvelteStack` / `Readable`) instead of an `Outlet`.

## Adapter pages

<CardGroup cols={4}>
  <Card
    title="Core"
    icon="/icons/vanilla.svg"
    href="/adapters/core"
    description="@stainless-code/layers (UI-agnostic engine)"
    arrow
  />
  <Card
    title="React"
    icon="/icons/react.svg"
    href="/adapters/react"
    description="@stainless-code/react-layers"
    arrow
  />
  <Card
    title="Preact"
    icon="/icons/preact.svg"
    href="/adapters/preact"
    description="@stainless-code/preact-layers"
    arrow
  />
  <Card
    title="Solid"
    icon="/icons/solid.svg"
    href="/adapters/solid"
    description="@stainless-code/solid-layers"
    arrow
  />
  <Card
    title="Angular"
    icon="/icons/angular.svg"
    href="/adapters/angular"
    description="@stainless-code/angular-layers"
    arrow
  />
  <Card
    title="Vue"
    icon="/icons/vue.svg"
    href="/adapters/vue"
    description="@stainless-code/vue-layers"
    arrow
  />
  <Card
    title="Svelte (runes)"
    icon="/icons/svelte.svg"
    href="/adapters/svelte/runes"
    description="@stainless-code/svelte-layers (Svelte 5.7+)"
    arrow
  />
  <Card
    title="Svelte (store)"
    icon="/icons/svelte.svg"
    href="/adapters/svelte/store"
    description="@stainless-code/svelte-layers/store (Svelte 3+)"
    arrow
  />
</CardGroup>

For lifecycle, blockers, and type inference, see [Concepts](/concepts). For a full walkthrough, see [Getting started](/guides/getting-started).
